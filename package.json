{
  "private": true,
  "scripts": {
    "start": "npm run build && npm run server",
    "server": "node server.js",
    "build": "browserify -t babelify streams.src.js > streams.js; jsx --harmony src .",
    "watch": "jsx --watch --harmony src ."
  },
  "dependencies": {
    "express": "^4.12.3",
    "influx": "^3.3.0",
    "peer": "^0.2.8",
    "peerjs": "^0.3.14",
    "react": "^0.13.3",
    "socket.io": "^1.3.5"
  },
  "devDependencies": {
    "babelify": "^6.0.2",
    "browserify": "^10.0.0",
    "react-tools": "^0.13.3"
  }
}
