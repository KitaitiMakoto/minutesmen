{
  "private": true,
  "scripts": {
    "build": "browserify -t babelify streams.src.js > streams.js"
  },
  "dependencies": {
    "express": "^4.12.3",
    "influx": "^3.3.0",
    "socket.io": "^1.3.5"
  },
  "devDependencies": {
    "babelify": "^6.0.2",
    "browserify": "^10.0.0"
  }
}
